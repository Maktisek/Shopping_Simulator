package UI.MainUI.ShopUI.Items;

/**
 * This enum represents an origin of an individual instance of {@link Items.Item}.
 * <p>
 *     It is used in {@link ItemUI} for proper and clean implementation of initialization.
 * </p>
 * @author Matěj Pospíšil
 * @since   1.0 - (pre-release version)
 */
public enum ItemSpecification {
    SHOP, NPC
}
