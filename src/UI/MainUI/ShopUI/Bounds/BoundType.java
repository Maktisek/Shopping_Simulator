package UI.MainUI.ShopUI.Bounds;

/**
 * This enum represents types of {@link BoundPanelUI} instances.
 * <p>
 *     It is chosen depending on which upgrade type should be displayed.
 * </p>
 * @author Matěj Pospíšil
 * @since   1.0 - (pre-release version)
 */
public enum BoundType {
    BUY_BOUND, SELL_BOUND, STOCK_BOUND
}
