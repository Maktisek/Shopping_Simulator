package UI.CreationUI.Buttons;

/**
 * This enum represents a type of button.
 * <p>
 *     It is made to determine, which sound should be done when the button is clicked.
 * </p>
 * @author Matěj Pospíšil
 * @since   1.0 - (pre-release version)
 */
public enum ButtonType {
    
    ENTER, EXIT, NONE
}
